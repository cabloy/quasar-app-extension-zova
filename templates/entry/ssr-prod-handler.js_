import serverEntry, { initialize, renderToString } from 'app/<%=__outDir__%>/server/server-entry.js';
import renderTemplate from './render-template.js';

export { initialize, renderTemplate, renderToString, serverEntry };
